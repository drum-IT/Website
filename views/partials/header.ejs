<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1,user-scalable=0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
		<link rel="stylesheet" href="/css/modern-normalize.css">
		<link href="https://fonts.googleapis.com/css?family=Ubuntu:400,700" rel="stylesheet">
		<link rel="stylesheet" href="/css/main.css">
		<title>Welcome</title>
		<script src="/js/main.js" defer></script>
  </head>

  <body style="display:none" onload="document.body.removeAttribute('style');">
    <header>
			<a class="brand" href="/"></a>
      <nav class="nav--big">
        <div class="nav__section">
					<a class="nav__link <%= page === 'landing' ? 'active' : ''%>" href="/">Home</a>
					<a href="/forums" class="nav__link <%= page === 'forum' ? 'active' : ''%>">Forums</a>
        </div>
        <div class="nav__divider"></div>
        <div class="nav__section">
          <% if (!user) { %>
          <a href="/login" class="nav__link <%= page === 'login' ? 'active' : ''%>">Sign In</a>
          <a href="/register" class="nav__link <%= page === 'register' ? 'active' : ''%>">Sign Up</a>
					<% } else { %>
						<div class="dropdown">
							<span class="nav__link dropdown__link">Signed in as <%= user.username %></span>
							<div class="dropdown__content">
								<a class="nav__link" href="#">My Profile</a>
								<a class="nav__link" href="/logout">Sign Out</a>
							</div>
						</div>
          <% } %>
        </div>
			</nav>

			<nav class="nav--small">
				<div class="dropdown">
					<i class="dropdown__icon fas fa-bars"></i>
					<div class="dropdown__content">
						<a class="nav__link <%= page === 'landing' ? 'active' : ''%>" href="/">Home</a>
						<a href="/forums" class="nav__link <%= page === 'forum' ? 'active' : ''%>">Forums</a>
						<div class="dropdown__divider--small"></div>
						<% if (!user) { %>
						<a href="/login" class="nav__link <%= page === 'login' ? 'active' : ''%>">Sign In</a>
						<a href="/register" class="nav__link <%= page === 'register' ? 'active' : ''%>">Sign Up</a>
						<% } else { %>
						<span class="nav__link">Signed in as
							<%= user.username %>
						</span>
						<a class="nav__link" href="#">My Profile</a>
						<a class="nav__link" href="/logout">Sign Out</a>
						<% } %>
					</div>
				</div>
			</nav>
    </header>

    <% if (error && error.length > 0) { %>
		<div id="alert" class="alert">
      <p class="alert--content alert--error"><%= error %></p>
    </div>
    <% } %>
    <% if (success && success.length > 0) { %>
    <div id="alert" class="alert">
      <p class="alert--content alert--success"><%= success %></p>
    </div>
    <% } %>